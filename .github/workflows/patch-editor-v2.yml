name: Patch editor v2

on:
  push:
    branches:
      - feature/prd-foundation-remotion
    paths:
      - scripts/patch-editor-v2.cjs

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/prd-foundation-remotion
      - run: node scripts/patch-editor-v2.cjs
      - shell: bash
        run: |
          if git diff --quiet; then exit 0; fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/app/Editor.tsx
          git commit -m "Stabilize editor layout and SVG controls"
          git push origin HEAD:feature/prd-foundation-remotion
